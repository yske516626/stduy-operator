#include<stdio.h>
int main(void)
{
	int a = 0;
	int b = 0;
	a = 3;
	b = 5;
	printf("为交换前：a = %d,b = %d\n", a, b);

	a = a ^ b;
	b = a ^ b;
	a = a ^ b;
	printf("交换后：a = %d,b = %d\n", a, b);
	return 0;
}
